# Front UPi (NAPSI) — copie para .env e ajuste só na sua máquina:
#   copy .env.example .env   (Windows)
#   cp .env.example .env     (Linux/macOS)
#
# Nenhum script do repositório cria, apaga ou sobrescreve o seu .env.

# Porta do Vite (npm run dev)
VITE_DEV_PORT=5173

# Porta usada pelo Playwright (npm run test:e2e)
PLAYWRIGHT_PORT=5173

# Dev: proxy Vite em vite.config.ts (/api → localhost:8000). Deixe vazio ou comente.
# VITE_API_BASE_URL=

# Produção (Vercel): URL pública da API no teu PC (Cloudflare + DDNS)
# VITE_API_BASE_URL=https://upi-api.patitow.dev
